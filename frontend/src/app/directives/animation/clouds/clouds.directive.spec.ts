import { CloudsDirective } from './clouds.directive';

describe('CloudsDirective', () => {
  it('should create an instance', () => {
    const directive = new CloudsDirective();
    expect(directive).toBeTruthy();
  });
});
